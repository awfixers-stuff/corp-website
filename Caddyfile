# Spin up the caddy server with `caddy run` or `yarn caddy`
# this is useful to test the github oauth flow locally

website.localhost {
  reverse_proxy :3000
}

cms.website.localhost {
  reverse_proxy :8001
}
